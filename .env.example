# Environment variables for development
NODE_ENV=development
PORT=5500
HOST=0.0.0.0
DATABASE_URL=postgresql://username:password@localhost:5432/your_database_name

# Note: Each team member will need to update DATABASE_URL with their actual database credentials
# For local development, you might use something like:
# DATABASE_URL=postgresql://postgres:password@localhost:5432/cse340